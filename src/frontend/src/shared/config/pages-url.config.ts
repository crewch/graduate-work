class DASHBOARD {
	private root = '/i'

	CONTESTS = `${this.root}/contests`
	LEADERBOARDS = `${this.root}/leaderboards`
	CREATE_CONTEST = `${this.root}/create-contest`
	CREATE_PROBLEM = `${this.root}/create-problem`
}

export const DASHBOARD_PAGES = new DASHBOARD()
